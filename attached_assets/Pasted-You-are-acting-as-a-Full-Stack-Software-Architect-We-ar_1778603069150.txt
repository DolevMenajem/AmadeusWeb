You are acting as a Full-Stack Software Architect. We are building an "AI Music Studio" web platform. This platform will host a suite of AI-driven musical services for users to interact with MIDI files and live MIDI inputs.

A core feature of this platform is the "AI Music Lecturer," which combines local PyTorch machine learning with an external LLM API (like Gemini or OpenAI) to provide educational feedback.

Core Tech Stack:

Backend: Python, FastAPI, Uvicorn (Modular structure using APIRouter)

LLM Integration: google-genai (or openai) and python-dotenv for API keys.

Frontend: React (Vite) or Next.js, using a modern, dark-mode aesthetic typical of professional DAWs (Digital Audio Workstations).

Please build the foundational scaffolding for this project adhering to these requirements:

1. Frontend UI / UX Architecture
Create a Dashboard layout featuring a persistent navigation menu. The user needs to easily switch between the following tool views:

Evaluate & Feedback (The Lecturer): Upload a .mid file. The UI should display the predicted Genre and Confidence visually (e.g., progress bars), alongside a stylized text box displaying the "Lecturer's Feedback".

Music Extension: Upload a .mid file, specify a target length, and request AI continuation.

Genre Shift: Upload a .mid file, select a target style (e.g., Classical to Jazz), and request a style transfer.

Live Feedback: A specialized view ready to accept streaming MIDI data and display real-time metrics.

Build out the UI components for these views. For now, they can hit the backend and display the mock data returned. Include loading states (spinners) and error handling.

2. Backend API Structure (FastAPI)
Do not put all logic in main.py. Use FastAPI's APIRouter to separate the services.

The Advanced /api/evaluate Endpoint:
This endpoint must execute a two-step process:

Local Extraction: Accept a multipart/form-data .mid file. Leave a placeholder where a PyTorch .pth model will predict the genre. Also, write a basic placeholder function that extracts "MIDI features" (e.g., mock tempo, total notes, unique pitches).

LLM Generation: Take the predicted genre and extracted features, format them into a prompt (e.g., "Act as a university music lecturer. Review a student's piece that is classified as [Genre] with [Tempo] BPM... "), and send it to an external LLM API.

Response: Return a JSON object containing {"genre": "Jazz", "confidence": 0.88, "features": {...}, "lecturer_feedback": "The syncopation in this jazz piece is..."}.

Other Endpoints (Mock Data):

POST /api/extend: Accepts a .mid file and an integer parameter. Returns a mock download link.

POST /api/shift: Accepts a .mid file and a string for target genre. Returns a mock success message.

Setup WebSockets:

WS /ws/live: Create a WebSocket endpoint designed to accept a stream of MIDI byte data, log it, and stream back a mock "current evaluation" string every few seconds.

3. PyTorch Integration Readiness
Create a folder named models/.

Inside, create an empty classifier_model.py.

Add torch, miditok, symusic, python-dotenv, and an LLM SDK to the requirements.txt.

In the /api/evaluate route, leave a clear comment block where a .pth file will be loaded into RAM using map_location=torch.device('cpu').

Please generate the filesystem, the frontend dashboard, and the modular FastAPI backend now. Include a .env.example file for the LLM API key.